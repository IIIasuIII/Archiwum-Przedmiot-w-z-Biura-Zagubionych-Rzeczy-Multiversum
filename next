#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef enum {
stabilny, 
chwiejny,
niestabilny,
nieklasyfikowany,
}stab;

typedef struct Node{
char nazwa[101];
char swiat[50];
int poziom_chaosu;
char opis[300];
stab stabilnosc;

struct Node* next;
}Node;

void print_list(Node* head) {

Node* current = head;

while(current!=NULL) {
printf("Nazwa: %s\n", curent->nazwa);
printf("Swiat: %s\n", curent->swiat);
printf("Poziom chaosu: %d\n", curent->poziom_chaosu);
printf("Opis: %s\n", curent->opis);
printf("stabilnosc: %d\n", curent->stabilnosc);

current = current->next;
}








} // to void












int main()
{



return 0;
}
